# =============================================================================
# Connector config for the TOON store box — rendered by ./render.sh
# =============================================================================
# The connector is the payment proxy in front of the store. It meters and
# settles the payment, then reverse-proxies the job to the store over plain
# HTTP. Everything the connector needs is in this one file.
#
# Three rules the parser enforces, worth knowing before you edit:
#
#   * One TOML file, read once at startup, immutable for the process
#     lifetime. There is NO environment-variable layer — the only variable the
#     connector reads is RUST_LOG. Editing this file does nothing until the
#     container restarts (a bind mount is not a reload).
#   * Unknown keys are a hard startup failure, and removed keys are refused by
#     name. That is the behaviour you want — a stale config says so at boot
#     instead of quietly doing nothing — but it means a config change and a
#     connector upgrade must land in the right order. Land config first.
#   * Every key that carries a secret is a PATH, never the value — the
#     operator credentials included. Nothing in this file is a credential, so
#     the rendered connector.toml is not a secret.
#
# ── This file targets an exact `rust-sha-` build ────────────────────────────
# `docker-compose.yml` pins the connector to one immutable `rust-sha-` tag.
# It used to follow the fleet's `:rust-release` pointer; nothing moves that
# pointer any more (connector ADR 0068 — a node repository pins the connector
# it runs, in one place, guarded there), so following it meant standing still
# on a build whose runtime peerings could accept but never pay (connector#1230).
#
# Bumping the pin is a reviewed commit here: change the image tag in
# docker-compose.yml and the matching literal in
# src/deploy-bundle-guard.test.ts together, land this file's changes FIRST if
# the new build wants a different schema, then move the tag. The parser is
# `deny_unknown_fields` and startup is fail-closed, so a schema drift under you
# is a refuse-to-start, never a degraded run.
#
# What the pinned build wants of this file, and why it looks the way it does:
#
#   * `price` may be a `{ base, per_kib }` table (ADR 0065). An older
#     connector parses only a bare integer, so this file will NOT load on a
#     build from before that -- it is a TOML parse error, i.e. a refuse-to-start.
#   * `[node]` replaced `[announce]`, and carries only the three keys below.
#     The one-shot `connector announce` verb is gone (ADR 0046); `GET /ilp`
#     serves the self-description instead, so there is no announce sidecar.
#   * `[[peers]] credential` is deleted (ADR 0060). A peering is proven by a
#     verified claim on a `[[peer_channels]]` row, not by a shared string.
#   * `[node]` endpoints are required exactly when `peer_expose` opens that
#     carriage (connector#1232). This file opens BTP and declares both.
#
# deploy/README.md § "Bumping the connector pin" has the procedure.
# src/deploy-bundle-guard.test.ts asserts this shape and the pin, so CI tells
# you if the file and the tag ever disagree.
# =============================================================================

# The client edge: POST /ilp, GET /ilp/btp, GET /ilp/identity and /metrics all
# share this port. Bound to 0.0.0.0 INSIDE the container only — docker-compose
# publishes it on 127.0.0.1, and nginx is what faces the internet.
client_edge_addr = "0.0.0.0:4000"

# Where this node records which claims it has already been paid. This MUST be a
# mounted volume: a watermark that dies with the container hands every payer
# their spent claims back as free service.
state_dir = "/app/state"

# Expose the peer wire over BTP (websocket), which is what the relay box dials.
peer_expose = "btp"

# This node's signing identity. `GET /ilp/identity` answers with this key, and
# every gift-wrapped packet is sealed to it.
[signer]
key_file = "/app/data/signer.key"


# ── What this node sells ─────────────────────────────────────────────────────
# One route per thing you serve. Longest matching prefix wins.
#
# `handler_url` is a RouteTermination: a paid packet for this prefix is
# delivered as a plain HTTP POST to that exact URL. The `/store` suffix is
# load-bearing — the store's backend serves POST /store, and a bare
# `http://store:3300` comes back as F99 "app declined the delivery with HTTP
# 404".
#
# `price` counts in the SMALLEST UNIT of the settlement token, the way a card
# terminal counts in cents. USDC has 6 decimals, so 1,000,000 units are $1.
#
# It may be a flat integer, or a SCHEDULE over the payload length (ADR 0065):
#
#     price = 1000                            flat: every packet, one price
#     price = { base = 1000, per_kib = 10 }   base, plus 10 per KiB of payload
#
# A blob store charges a schedule, because an upload can be any size and a
# flat price bills a 50 MB object the same as a 1 KB one. `per_kib` is charged
# per whole kibibyte, rounded up: base + per_kib * ceil(payload_len / 1024).
#
#       1 KB ->      1,010          1 MB ->     11,240
#     100 KB ->      2,000         50 MB ->    513,000   (~$0.51)
#
# ~$10.7/GB, which tracks what permanent Arweave storage costs plus a margin.
#
# This is the seam to copy for your own app: point handler_url at your
# backend, pick your prefix, pick your price.
[[routes]]
prefix      = "g.toon.store"
handler_url = "http://store:3300/store"
price       = { base = 1000, per_kib = 10 }

# The same app under the relay's name for it. The relay forwards
# `g.toon.relay.store` here over the peering it established, and a forward
# copies the destination through verbatim — so those packets arrive addressed
# to this prefix and need a route of their own, or they are refused F02 at the
# door. Priced identically to `g.toon.store` because the two share a
# `handler_url`, and the connector refuses a config where one handler is
# reachable at two prices (the cheaper door would take every packet). This is
# the one deliberate second name: it is the relay's, not an alias of ours.
[[routes]]
prefix      = "g.toon.relay.store"
handler_url = "http://store:3300/store"
price       = { base = 1000, per_kib = 10 }


# ── Getting paid ─────────────────────────────────────────────────────────────
# Settlement is per-chain. A client pays in whichever of these it holds; the
# connector verifies the claim against the chain named here.

[settlement.evm]
rpc_url          = "https://base-sepolia-rpc.publicnode.com"
contract_address = "0x0c41D9D424d6B075A3cEa1068a694f7847a8CCa5"  # TokenNetworkRegistry
token_address    = "0x49beE1Bca5d15Fb0963117923403F9498119a9Ce"  # USDC (6dp)
decimals         = 6

[settlement.evm.key]
key_file = "/app/data/settlement.key"

[settlement.solana]
rpc_url       = "https://api.devnet.solana.com"
program_id    = "2aEVJ8koKD8LTZrLRSGtAtU7LBt4e7QjjCgf1kzQ7Rip"
token_address = "34eSxY7qxQ4GzyhDJ8GpUcTz1WWzruGbJbR8q6TtxfQU"   # mock USDC (6dp)
decimals      = 6

[settlement.solana.key]
key_file = "/app/data/settlement-solana.key"


# ── The operator surface ─────────────────────────────────────────────────────
# Gates /metrics and the redeem endpoints. Without this section they are not
# mounted at all (404, not 401) — which also means no way to redeem what you
# have earned, so it is not optional in practice.
#
# Both are FILE-VALUED, like every other credential this file names. The
# values live in two files render.sh writes from .env (`OPERATOR_BEARER_TOKEN`
# and `OPERATOR_WRITE_KEY`), 0600 and owned by uid 10001, mounted beside the
# key files — so the rendered connector.toml holds paths and nothing else. A
# section naming a file that is missing or empty is a refuse-to-start, never a
# silently open surface.
#
# `write_keys_file` is the allowlist of RFC 9421 signing keys for operator
# WRITES — `POST /peers`, `POST /routes/peers`, `POST /channels/:id/fund` —
# one PUBLIC key per line, `#` comments allowed. The private half never lives
# on this box; to sign a write from a shell, use the connector repo's
# docs/operators/sign-write.sh. Revoke a key by deleting its line and
# restarting.
[operator]
bearer_token_file = "/app/data/operator-bearer.token"
write_keys_file   = "/app/data/operator-write.keys"


# ═════════════════════════════════════════════════════════════════════════════
# THIS DEPLOYMENT — the TOON devnet store box
# ═════════════════════════════════════════════════════════════════════════════
# Everything above describes any store behind any connector. What follows is
# specific to this box's place in the TOON devnet — the addresses it answers
# for and the URLs it is reachable on — and is what you replace when you stand
# up your own. See deploy/README.md § "Make it yours".

# ── The relay leg ────────────────────────────────────────────────────────────
# This node has NO route to the relay, and that is deliberate.
#
# A forward copies the destination through verbatim — `forward_via_peer_route`
# builds `Prepare { amount, expires_at, ..prepare }` and changes nothing else.
# So a forwarded prefix only reaches anywhere if the far side already routes
# that exact name. A prefix beneath THIS node's address, such as
# `g.toon.store.relay`, is not a name the relay routes and must not become one:
# it would put a row for this node's naming in the relay's table.
#
# The traffic that matters goes the other way. The relay carries packets TO
# this node — addressed `g.toon.store`, or `g.toon.relay.store`, the name the
# relay's own routing table forwards under (both terminated above) — and this
# node terminates them. `peer_expose` is what lets that arrive: the relay dials, and
# a peering is accepted here rather than opened from here.
#
# If a route out ever is wanted, it takes the relay's OWN address as its prefix
# and is established through the operator surface (`POST /peers`, then
# `POST /routes/peers` — ADR 0058), never written here. A runtime write is
# refused outright when the config file owns the key (CF-32), and ownership is
# permanent (CF-33), so writing one here would take it from that surface for
# good.

# What this node cannot introspect about itself: the addresses it answers for
# and the public URLs it is reachable on. Clients read this back from
# `GET /ilp` to discover the node.
[node]
addresses     = ["g.toon.store", "g.toon.relay.store"]
http_endpoint = "https://proxy.ario.devnet.toonprotocol.dev/ilp"
btp_endpoint  = "wss://proxy.ario.devnet.toonprotocol.dev/ilp/btp"
