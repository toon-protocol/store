# =============================================================================
# Connector config for the TOON store box — rendered by ./render.sh
# =============================================================================
# The connector is the payment proxy in front of the store. It meters and
# settles the payment, then reverse-proxies the job to the store over plain
# HTTP. Everything the connector needs is in this one file.
#
# Three rules the parser enforces, worth knowing before you edit:
#
#   * One TOML file, read once at startup, immutable for the process
#     lifetime. There is NO environment-variable layer — the only variable the
#     connector reads is RUST_LOG. Editing this file does nothing until the
#     container restarts (a bind mount is not a reload).
#   * Unknown keys are a hard startup failure, and removed keys are refused by
#     name. That is the behaviour you want — a stale config says so at boot
#     instead of quietly doing nothing — but it means a config change and a
#     connector upgrade must land in the right order. Land config first.
#   * Every key that carries a secret is a PATH, never the value. The one
#     exception is [operator] below, and that is a limitation of the pinned
#     connector rather than a preference.
#
# ── This file targets `:rust-release` ────────────────────────────────────────
# `docker-compose.yml` pins the connector to the fleet's promotion tag
# `:rust-release`, which now carries ADR 0065 (a price is a schedule over
# payload length) and ADR 0050/0060.
#
# What that means for this file, and why it looks the way it does:
#
#   * `price` may be a `{ base, per_kib }` table. An older connector parses
#     only a bare integer, so this file will NOT load on a build from before
#     the promotion -- it is a TOML parse error, i.e. a refuse-to-start.
#   * `[node]` replaced `[announce]`, and carries only the three keys below.
#     The one-shot `connector announce` verb is gone (ADR 0046); `GET /ilp`
#     serves the self-description instead, so there is no announce sidecar.
#   * `[[peers]] credential` is deleted (ADR 0060). A peering is proven by a
#     verified claim on a `[[peer_channels]]` row, not by a shared string.
#
# deploy/README.md has the full record. src/deploy-bundle-guard.test.ts
# asserts this shape, so CI tells you if the file and the tag ever disagree.
# =============================================================================

# The client edge: POST /ilp, GET /ilp/btp, GET /ilp/identity and /metrics all
# share this port. Bound to 0.0.0.0 INSIDE the container only — docker-compose
# publishes it on 127.0.0.1, and nginx is what faces the internet.
client_edge_addr = "0.0.0.0:4000"

# Where this node records which claims it has already been paid. This MUST be a
# mounted volume: a watermark that dies with the container hands every payer
# their spent claims back as free service.
state_dir = "/app/state"

# Expose the peer wire over BTP (websocket), which is what the relay box dials.
peer_expose = "btp"

# This node's signing identity. `GET /ilp/identity` answers with this key, and
# every gift-wrapped packet is sealed to it.
[signer]
key_file = "/app/data/signer.key"


# ── What this node sells ─────────────────────────────────────────────────────
# One route per thing you serve. Longest matching prefix wins.
#
# `handler_url` is a RouteTermination: a paid packet for this prefix is
# delivered as a plain HTTP POST to that exact URL. The `/store` suffix is
# load-bearing — the store's backend serves POST /store, and a bare
# `http://store:3300` comes back as F99 "app declined the delivery with HTTP
# 404".
#
# `price` counts in the SMALLEST UNIT of the settlement token, the way a card
# terminal counts in cents. USDC has 6 decimals, so 1,000,000 units are $1.
#
# It may be a flat integer, or a SCHEDULE over the payload length (ADR 0065):
#
#     price = 1000                            flat: every packet, one price
#     price = { base = 1000, per_kib = 10 }   base, plus 10 per KiB of payload
#
# A blob store charges a schedule, because an upload can be any size and a
# flat price bills a 50 MB object the same as a 1 KB one. `per_kib` is charged
# per whole kibibyte, rounded up: base + per_kib * ceil(payload_len / 1024).
#
#       1 KB ->      1,010          1 MB ->     11,240
#     100 KB ->      2,000         50 MB ->    513,000   (~$0.51)
#
# ~$10.7/GB, which tracks what permanent Arweave storage costs plus a margin.
#
# This is the seam to copy for your own app: point handler_url at your
# backend, pick your prefix, pick your price.
[[routes]]
prefix      = "g.toon.ario"
handler_url = "http://store:3300/store"
price       = { base = 1000, per_kib = 10 }


# ── Getting paid ─────────────────────────────────────────────────────────────
# Settlement is per-chain. A client pays in whichever of these it holds; the
# connector verifies the claim against the chain named here.

[settlement.evm]
rpc_url          = "https://base-sepolia-rpc.publicnode.com"
contract_address = "0x8263BdD4eB4862395Cb4ef5dA5d637F4b047Eea1"  # TokenNetworkRegistry
token_address    = "0x49beE1Bca5d15Fb0963117923403F9498119a9Ce"  # USDC (6dp)
decimals         = 6

[settlement.evm.key]
key_file = "/app/data/settlement.key"

[settlement.solana]
rpc_url       = "https://api.devnet.solana.com"
program_id    = "2aEVJ8koKD8LTZrLRSGtAtU7LBt4e7QjjCgf1kzQ7Rip"
token_address = "xyc5J8MgKFiEN13PnfftdXxUzYH34FEvw1LCrFwN7in"   # mock USDC (6dp)
decimals      = 6

[settlement.solana.key]
key_file = "/app/data/settlement-solana.key"


# ── The operator surface ─────────────────────────────────────────────────────
# Gates /metrics and the redeem endpoints. Without this section they are not
# mounted at all (404, not 401) — which also means no way to redeem what you
# have earned, so it is not optional in practice.
#
# On the pinned tag these are INLINE values, so the rendered connector.toml is
# a secret: render.sh writes it 0600 and .gitignore keeps it out of git. Only
# the template is committed. (On a newer connector they become *_file keys and
# this stops being true.)
[operator]
bearer_token = "${OPERATOR_BEARER_TOKEN}"
write_keys   = ["${OPERATOR_WRITE_KEY}"]


# ═════════════════════════════════════════════════════════════════════════════
# THIS DEPLOYMENT — the TOON devnet store box
# ═════════════════════════════════════════════════════════════════════════════
# Everything above describes any store behind any connector. What follows is
# specific to this box's place in the TOON devnet — the addresses it answers
# for and the URLs it is reachable on — and is what you replace when you stand
# up your own. See deploy/README.md § "Make it yours".

# ── Peering with the relay box ───────────────────────────────────────────────
# The relay is the fleet's write ingress. Peering with it puts `g.toon.relay`
# in this node's routing table, so a client already connected here can pay
# THIS edge to reach it instead of opening a second channel of its own.
#
# `endpoint`'s scheme selects the carriage — `wss://` is BTP. A row with no
# endpoint would be accept-only; this one dials out.
#
# `max_packet_amount` is the cap: the largest amount forwarded to this peer in
# one packet (ADR 0042). There is no "unbounded" spelling, deliberately, and
# `0` is refused by name rather than read as "disabled".
#
# `fee` is what this connector retains for carrying one packet there (ADR
# 0061). It attaches to the peering, not to a route, because carrying a packet
# is the same work whichever prefix was addressed.
[[peers]]
id                = "relay"
endpoint          = "wss://proxy.relay.devnet.toonprotocol.dev/ilp/btp"
max_packet_amount = 100000
fee               = 1

# The channel the relay's claims are JUDGED against, and the key whose
# signature is accepted on it. Nothing here authenticates the peering by a
# shared string — ADR 0060 deleted that outright; the role is proved by this
# binding plus a verified claim signature.
[[peer_channels]]
peer_id          = "relay"
channel_id       = "0x53689fa291bc99f1b94574adaf198494bc895963052e113922329f3c8bae792d"
counterparty_key = "0x3f43d923a611bcb2d0bfb5d6ee2c3ac3efeaf308"
chain_id         = 84532
token_network    = "0xa79c3b1dbcea00a6d84735a134395d8ef6d6a478"

# The channel this node PAYS FROM when it forwards to the relay: every
# forwarded PREPARE carries a covering claim on it (ADR 0042). Deliberately
# the SAME channel as the peer row above — CF-22 permits one channel in both
# roles, the peer role judging what arrives and the pay role covering what
# this node sends, and it is the deployed shape here: one hop, one channel,
# funded from both sides.
#
# `client_edge_url` is where those claims are presented. The relay exposes no
# peer carriage of its own, so this node pays it as an ordinary client — which
# is what it already did to publish its announce.
[[pay_channels]]
peer_id         = "relay"
channel_id      = "0x53689fa291bc99f1b94574adaf198494bc895963052e113922329f3c8bae792d"
chain_id        = 84532
token_network   = "0xa79c3b1dbcea00a6d84735a134395d8ef6d6a478"
client_edge_url = "https://proxy.relay.devnet.toonprotocol.dev/ilp"

# The routing-table entry the peering exists for. A route is a prefix plus
# EXACTLY ONE of `handler_url` (terminate here) or `peer_id` (forward there),
# and a price is required on both branches.
#
# A forwarded route is priced at THIS client edge (ADR 0028): the relay
# charges 1 for `g.toon.relay`, this node retains its `fee` of 1, so a client
# pays 2 here. Longest prefix wins, so `g.toon.ario` above still terminates
# locally and is unaffected.
[[routes]]
prefix  = "g.toon.relay"
peer_id = "relay"
price   = 2

# What this node cannot introspect about itself: the addresses it answers for
# and the public URLs it is reachable on. Clients read this back from
# `GET /ilp` to discover the node.
[node]
addresses     = ["g.toon.ario"]
http_endpoint = "https://proxy.ario.devnet.toonprotocol.dev/ilp"
btp_endpoint  = "wss://proxy.ario.devnet.toonprotocol.dev/ilp/btp"
