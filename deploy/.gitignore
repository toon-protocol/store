# Secrets and rendered output. Only the templates are committed.
.env
.env.*
!.env.example
connector.toml
# The last commit auto-apply.sh applied AND verified (TOON_Network#164).
.applied
operator-bearer.token
operator-write.keys
nginx/conf.d/
*.key
*.secret
